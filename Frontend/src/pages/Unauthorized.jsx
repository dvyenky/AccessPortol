import { Box, Typography } from "@mui/material";

const Unauthorized = () => {
  return (
    <Box p={4}>
      <Typography variant="h4">
        Unauthorized Access
      </Typography>
    </Box>
  );
};

export default Unauthorized;